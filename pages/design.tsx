import * as React from 'react';
import { Grid, Box } from '@material-ui/core';

const DesignPage = (): React.ReactElement => (
    <Grid item>
        <Box textAlign="center">Design</Box>
    </Grid>
);

export default DesignPage;
